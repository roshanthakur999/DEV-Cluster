# Dev Python ECS Sample App

A simple Flask app deployed on AWS ECS via Jenkins pipeline.

## Run locally

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
